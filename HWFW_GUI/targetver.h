#pragma once

// Including SDKDDKVer.h will define the highest version of the Windows platform available.

// If you are building applications for previous Windows platforms, include WinSDKVer.h, and add
// Set the _WIN32_WINNT macro to the platform to support before including SDKDDKVer.h.

#include <SDKDDKVer.h>
